# To convert to a integer, use int()
age = int(input("How old are you?\n"));

# // is a WHOLE NUMBER operator.
decades = age // 10;
years = age % 10;

# To convert to a string use str()
print("You are " + str(decades) + " decades and " + str(years) + " years old!");